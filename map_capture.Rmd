---
pagetitle: "Carte - Map"
output: html_document
---

\

```{r map_capture, echo=FALSE, message=FALSE, warning=FALSE}
require(tidyverse); require(sf); require(knitr); require(leaflet)

secteurs=read.csv("fichiers/secteurs.csv", header=TRUE, dec=".", sep=";")

secteurs_sf =secteurs %>%
  st_as_sf(coords = c("longitude", "latitude"), crs = 4326) #What does crs mean?

leaflet(secteurs,width = "100%") %>% 
  addTiles(options = providerTileOptions(minZoom=4, maxZoom=8)) %>% 
  addCircleMarkers(
    radius = 15,
    color = "orange",
    stroke = FALSE,
    fillOpacity = 0.5,
    popup= paste("Région / Region : ", secteurs$what,"<br>",
                 secteurs$veaux, "veaux capturés / calves captured", "<br>",
                 secteurs$adultes,"adulte.s capturé.s / adult.s captured", "<br>")
  ) %>%  
  setView(lat=47.2110091, lng=-70.7304825, zoom=6) %>% 
  leafem::addMouseCoordinates() %>% 
  addMiniMap(
    tiles = providers$Esri.WorldStreetMap,
    toggleDisplay = FALSE)


```
<p style="font-size:16px" > Carte interactive des cinq secteurs de captures de veaux et de femelles adultes pour l'hiver 2019. Cliquez sur un point pour obtenir davantage d'information. Dernière mise à jour = 25-02-2020. - *Interactive map of the five regions where calf and adult female moose were captured at winter 2019. Click on a location for more information. Last update = 25-02-2020* </p> 

